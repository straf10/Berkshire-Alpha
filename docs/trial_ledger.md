# Trial ledger

Every recorded revision to a trading parameter or scoring term, reconstructed from
`git log --all -p -- agent/config.py agent/strategy/ticker_screener.py` and the inline comments
those commits left in place. This is the trial count `N` that `agent/backtest/dsr.py` feeds to
`deflated_sharpe` — see docs/report.md §1 ("N3 — count the trials") for why an unrecorded search
makes a Sharpe number unusable, and `docs/preregistration.md` for the window this ledger justifies
sealing.

Dates are commit-author dates (`git log --date=short`), not session dates. Commit hashes are short
and resolve against this repo's history. Rows are ordered chronologically; each row is one
recorded trial, whether or not the resulting value differs from what shipped before it.

| # | param | old value | new value | date / commit | rationale |
|---|---|---|---|---|---|
| 1 | `VWM_Z_STRONG` | *(introduced)* | `1.0` | 2026-08-29, `ef73f94` | Initial value at first commit of `agent/config.py`, no prior measurement behind it. |
| 2 | `VWM_Z_STRONG` | `1.0` | `0.75` | 2026-08-30, `20fa1f9` | "max observed \|z\| was 0.80" — the 1.0 bar admitted almost nothing on the then-current universe. |
| 3 | `CROSS_SECTION_N` | *(introduced)* | `3` | 2026-08-30, `20fa1f9` | Introduced alongside cross-sectional VRP ranking (`assign_regimes`) replacing the old absolute `VRP_CREDIT_MIN`/`VRP_DEBIT_MAX` entry thresholds — see row 12. |
| 4 | `VWM_Z_STRONG` | `0.75` | `0.75` (considered `0.45`, reverted) | 2026-08-30/31, sourced from inline comment only — no git diff exists because the shipped value never changed | An earlier draft proposed lowering to 0.45 on the grounds that "no DEBIT candidate has ever cleared the bar." `scripts/vwm_sensitivity.py` (50 names × 212 sessions = 10,600 name-days) showed the true issue was a 10-name universe with 3 debit slots, not the bar: 17 of 75 `data_ok` snapshots cleared 0.75 but weren't in the bottom-`CROSS_SECTION_N` VRP slice on those days. The correction is recorded directly in `agent/config.py`'s comment above `VWM_Z_STRONG` because the value it corrects to (0.75) is identical to what preceded it — this is the second of the "two revisions" the report cites, and it is a trial (an alternative was tested and rejected) even though no line in the file changed. |
| 5 | `SKEW_PUT_BIAS_POINTS` | `5.0` (fixed constant) | *(removed)*, replaced by `ticker_screener.skew_threshold()` (cross-sectional 70th-percentile `skew_abs`) | 2026-08-30, `08d1b20` | Observed `skew_abs` across the universe never exceeded ~1.4 points, so a fixed 5.0-point gate made the skew overlay branch in `regime.select` structurally unreachable. |
| 6 | composite score skew term, divisor | `/ 10.0` | `/ 5.0` | 2026-09-01, `f0f279a` | Paired with row 7 — see rationale there. |
| 7 | composite score skew term, weight | `0.30` | `0.10` | 2026-09-01, `f0f279a` | `skew_abs`'s SIGN carries no information (median +0.06 IV points, 47% of readings negative over 75 `data_ok` snapshots) — the term switched to `abs(skew_abs)` and had its weight cut so a noisy magnitude signal doesn't dominate the composite. |
| 8 | `SKEW_SIDE_MIN_POINTS` | *(introduced)* | `1.5` | 2026-09-01, `f0f279a` | Same measurement as rows 6–7 (median +0.06, 47% negative): below this floor `regime.select`'s `SKEW_SIDED_NO_DIRECTION` branch falls back to the VWAP-based read instead of trusting an uninformative sign. |
| 9 | `CROSS_SECTION_N` | `3` | `4` | 2026-09-01, `f15dd3d` | Step 2, partition-ceiling assert (`2n <= len(UNIVERSE)`) added alongside; still on the 10-name universe. |
| 10 | `CROSS_SECTION_N` | `4` | `6` | 2026-09-01, `0377b5a` | Paired with row 13 (universe widened 10 -> 50): 6 is 12% of 50, "comfortably inside the ceiling" per the partition argument documented at the constant's definition. |
| 11 | `SHORTLIST_MAX` | `4` | `8` | 2026-09-01, `0377b5a` | Raised to exceed `DEBATE_CANDIDATES` (4) so `select_top`'s analyst-score ranking discards the worse half of the shortlist instead of selecting all 4 of at most 4 candidates. |
| 12 | VRP entry rule | `VRP_CREDIT_MIN = 1.25` (absolute threshold) | `VRP_CREDIT_MIN = 1.00`, used only as a cross-sectional sign guard in `assign_regimes`, not an absolute entry gate | 2026-08-30, `20fa1f9` | A 4-day sample's median VRP was 0.96 against the old 1.25 credit threshold — the absolute gate rejected almost everything, so entry became a cross-sectional rank instead. |
| 13 | `UNIVERSE` | 10 names (`SPY, QQQ, AAPL, MSFT, NVDA, AMD, TSLA, META, AMZN, GOOGL`) | 50 names | 2026-09-01, `0377b5a` | Selected on measured 3–7 DTE chain liquidity (`scripts/probe_universe.py`), ordered tightest median bid/ask spread first; widening was needed to give `CROSS_SECTION_N=6` (row 10) and the debit regime enough candidates to be reachable at all (see row 4). |
| 14 | `ANALYST_SCORE_FLOOR` | *(introduced)* | `0.40` | 2026-09-01, `f0f279a` | Smallest floor that rejects every analyst-score combination where the quant component is 0 (quant contradicts the chosen structure on both momentum and IV), plus the one case where quant is neutral and news actively disagrees. A candidate with no analyst opinion scores exactly 0.50 and still clears it — this is a veto on contrary evidence, not an authoriser. |
| 15 | `CONVICTION_UNANIMOUS_DISAGREE_FLOOR` | unanimous DISAGREE = absolute veto (conviction forced to 0.0) | `CONVICTION_UNANIMOUS_DISAGREE_FLOOR = 0.34` (size floor, not a veto) | 2026-08-31, `bc695f2` | Task 0 diagnosis (dry-run + historical DB query across 5 unanimous-DISAGREE vetoes on TSLA/AAPL) found the veto was prompt-driven, not evidence-driven — the BULL persona mirrored the BEAR's caution language even when rich IV favored the credit structure under evaluation. Converted to a floor; the deterministic gate can still reject as LOW_CONVICTION downstream. |
| 16 | `MACRO_RETURN_LOOKBACK` | single horizon (undated — no committed single-horizon version exists; see note) | `MACRO_RETURN_LOOKBACK_FAST_D = 1`, `MACRO_RETURN_LOOKBACK_SLOW_D = 5` | 2026-09-01, `700a215` | Config comment: "REVISED after measurement (§3.2a). TWO horizons, not one: the 1-day leg is a shock detector, the 5-day leg is the regime. A 5-day window alone masks a late-window reversal... a 1-day window alone fires on noise." `git log -S"MACRO_RETURN_LOOKBACK"` finds only the dual-horizon commit — the single-horizon draft this revised was never itself committed, so its old value cannot be sourced from git and is not invented here. |
| 17 | `KELLY_FRACTION` | `0.5` | `0.25` | 2026-09-02, `ae62f0d` | P1 remediation (docs/audit_report_v2.md §9 item 7): 0 wins in 2 closed trades plus one execution catastrophe is not a measured edge that justifies half-Kelly. Stopgap pending real sample size. |
| 18 | `VWM_Z_STRONG` | `0.75` | `1.00` | 2026-09-02, `ae62f0d` | Re-run over the LLY trades: both 2026-09-01 LLY entries cleared the old 0.75 bar by a margin of 0.011 — the thinnest possible admission, on the worst-liquidity chain in the universe. |
| 19 | `MAX_NET_SPREAD_WIDTH_PCT` | *(introduced)*, preceded by `1ef1cdd`'s 2026-09-02 decoupling of the per-leg width filter from `DEGENERATE_CHAIN`/position pricing | `0.50` | 2026-09-02 / 2026-09-09, `1ef1cdd` / `5062f59` | Width-filter decoupling: on 2026-09-08's 10 approved trades, every trade still EV-positive at the full natural price was ≤46% net width; every trade EV-negative at natural (GS, ARM, UNH) was ≥88%. 0.50 sits in the gap with margin on both sides. |
| 20 | `EV_RETENTION` | *(introduced)* | `0.50` | 2026-09-09, `5062f59` | Replayed 09-08's 10 trades, counting additional fills: the fraction of a plan's own modelled edge (at mid) the walk must preserve while spending the rest buying a fill. Replaying under this rule, 7 of the 10 trades (including QCOM x2) reach a marketable price; the 3 already EV-negative at natural (GS, ARM, UNH) are still correctly refused. |
| 21 | `WALK_MIN_STEPS` | *(introduced)* | `4` | 2026-09-09, `5062f59` | Fill-count driven: a 3-cent budget against a 5-cent `WALK_STEP` produced zero steps (limit + `WALK_STEP` > cap never true once the cap is only 3 cents from mid) — the walk cancelled without ever improving its price. Floors the step count so a thin budget still spends it. |
| 22 | `WALK_REQUOTE_EVERY_STEPS` | *(introduced)* | `3` | 2026-09-09, `5062f59` | Fill-count driven: the walk priced off a quote snapshot up to ~10 minutes stale by the time it gave up (measured 2026-09-08). Re-fetches and recomputes mid/natural/cap every 3 replace steps (~45s at `WALK_REST_S=15`). |
| 23 | `MIN_HOLD_S` | *(introduced)* | `900.0` | 2026-09-09, `5062f59` | Re-examined a stop-out: a position was stopped out 6m23s after entry on a single noisy mid read off a chain that was 23% wide at entry — not evidence against an 86%-probability position with three days left to work. No `STOP_LOSS` in the first 15 minutes of a position's life (`UNWIND`/`TIME_STOP_2DTE` remain exempt, as risk controls rather than P&L rules). |
| 24 | `STOP_CONFIRM_TICKS` | *(introduced)* | `2` | 2026-09-09, `5062f59` | Re-examined the same stop-out: the stop condition must hold on 2 consecutive management ticks (~`MANAGEMENT_INTERVAL_S` apart) before the position actually closes, so a single bad mark cannot terminate a position by itself. |
| 25 | `MAX_ENTRY_RETRY_ATTEMPTS` | *(introduced)* | `3` | 2026-09-10, `09e6e05` | Fill-rate driven: on 2026-09-09 AAPL was independently re-approved 3x from scratch (14:15, 15:47, 17:16 UTC), each pass burning a full funnel/LLM budget to re-derive the identical plan. Caps how many scans a pending unfilled entry gets re-quoted and re-attempted before being dropped for the rest of the session — still re-passes every risk gate each time; a retry budget, not a bypass. |
| 26 | `sizing.p_success()`'s breach-probability transform | `d_phys = d_rn / max(vrp_ratio, 0.5)` (linear) | `d_phys = Phi(vrp_shrunk * Phi^-1(d_rn))` (lognormal-consistent inverse-CDF sandwich) | 2026-09-10, `e356537` | Structural argument, not a marks-driven fit (docs/strategy_audit_and_loop.md §2/§4 P1): scaling vol by `1/vrp_ratio` under a lognormal underlying maps the breach quantile through `Phi(vrp * Phi^-1(d_rn))`, not linear division — the two forms agree exactly at `vrp_ratio == 1` and otherwise disagree by up to 3.40pp on the audit's 14 live plans, flipping EV sign on 3 of them, with the error changing sign near the middle of `SHORT_DELTA_BAND`. `scripts/signal_forward_test.py` cannot validate a probability transform against an IV surface (chain-free by design) — validated instead, going forward, by `scripts/p_success_validation.py` against genuinely settled trades as the 09-11/09-14/09-16 cohorts close. |
| 27 | `sizing.p_success()`'s vrp_ratio clamp | floor only, `max(vrp_ratio, 0.5)`, no ceiling | floor 0.5 (`VRP_RATIO_FLOOR`) + ceiling 2.0 (`VRP_RATIO_CEILING`, log-symmetric mirror of the floor) + shrink 50% toward 1.0 (`VRP_SHRINKAGE_FACTOR`) | 2026-09-10, `e356537` | docs/strategy_audit_and_loop.md §2 finding 3: the un-ceilinged floor let IWM's observed 1.59 cut breach probability by 37% off one noisy point estimate, unchecked. Both new constants are identity operations at `vrp_ratio == 1` (never disturb a fairly-priced spread) and are explicitly a trial, not a fitted calibration — logged pending `scripts/p_success_validation.py` accumulating enough settled outcomes to check it, same as row 26. |
| 28 | `vrp_ratio`'s realized-vol denominator | `RV_20` (fixed 20-day trailing window, `quant.realised_vol_20`) | DTE-matched window (`quant.realised_vol_dte(closes, dte)`) — `rv_20` itself (spread_builder's sqrt-time strike placement, main.py's debug print) is unchanged, only what feeds `vrp_ratio` | 2026-09-10, `e356537` | docs/strategy_audit_and_loop.md §2 finding 1 (Horizon mismatch): `IV_ATM` is priced for the position's own 3–7 DTE, so comparing it against a 20-day trailing RV understates forward vol precisely during vol expansion. Tested with `scripts/signal_forward_test.py`'s new RV_20-vs-DTE-matched comparison against realized forward move magnitude (50 symbols, 2024-09-10→2026-09-10, `agent/backtest/output/signal_forward_test_rv_horizon.csv`): DTE-matched RV cut systematic positive bias at every horizon (3 DTE: `+0.0049 -> -0.0026`; 7 DTE: `+0.0069 -> +0.0039`) but had HIGHER mean absolute error than RV_20 at every horizon (more variance, a real cost of the shorter window) — kept anyway because row 27's shrinkage exists specifically to control that added variance, and the underlying structural mismatch argument (comparing a 3–7 DTE-priced quantity to a 20-day backward estimate) doesn't depend on this one sample's bias-vs-MAE trade-off. Not an unambiguous win; said so rather than only reporting the number that favors the change. |
| 29 | `CROSS_SECTION_N` | `6` | `4` | 2026-09-10 | docs/strategy_audit_and_loop.md S1/S2: the "6 is 12% of 50" derivation (row 10) used a denominator that is never the real candidate pool — the 3-7 DTE band crossed with the weekly expiry calendar collapses the live universe to 13 names on Wednesdays/Thursdays (40% of sessions, confirmed on three live sessions), where `n=6` held out only 1 of 13 names (2*6=12<=13), very nearly a pass-through (S2 measured 12 of 13 names receiving a regime that day). `n=4` holds out 5 of 13 even on the worst day, at the accepted cost of tightening the good-day (~47-name) selectivity from 12.8% to 8.5% per side — a single static `n` cannot equalize selectivity across a universe that oscillates 13-47 by weekday; only `DTE_MAX` (deferred) removes the oscillation itself. Honest compromise, not a fix of S2. |
| 30 | `VWM_Z_STRONG` | `1.00` | `0.75` | 2026-09-10 | docs/strategy_audit_and_loop.md S3/S4 P3, decided from `scripts/vwm_sensitivity.py` alone (chain-free, re-run this date: 50 names x 212 sessions = 10,600 name-days, median \|vwm_z\| 0.644, bar 0.75 admits 43.5% of the tape). The 1.00 bar (row 18) rejected 8 of 8 real DEBIT candidates on 2026-09-09 (max \|vwm_z\| observed 0.594) — not marginal admissions like the LLY trades row 18 was reacting to, a complete closure of the branch this constant gates (`regime.py:97`). The walk-cap defect that motivated raising it to 1.00 is independently fixed (`WALK_CAP_MAX_FRACTION_OF_WIDTH`/`WALK_CAP_CREDIT_SIGN_FLOOR`), so the coincidental protection is no longer needed. |
| 31 | `VRP_DEBIT_MAX` | `1.00` | `1.00` (considered a resize alongside row 30, rejected) | 2026-09-10 | Paired with row 30's task (docs/strategy_audit_and_loop.md §4 P3 asks for both). Unlike `VWM_Z_STRONG`, this is an IV/RV ratio — `scripts/vwm_sensitivity.py` is chain-free by construction (no IV), and Alpaca has no historical options-chain endpoint (`replay.py`'s own docstring), so no live, chain-free instrument can measure it. Left at its structurally-neutral value (1.00 = no premium either direction) rather than fit it off `replay.py`'s still-partially-synthetic chain or the live marks §0 already disqualified. Same "an alternative was considered and rejected counts as a trial" logic as row 4. |
| 32 | `MAX_NET_SPREAD_WIDTH_PCT` formula/denominator | `(net_natural - net_bidside) / abs(net_mid)`, threshold `0.50` | unchanged — re-checked, not revised | 2026-09-10 | docs/strategy_audit_and_loop.md §2 row 9 flagged GS (decision_id 784, `BULL_PUT_SPREAD`, $1.33 mid-to-natural gap on a $2.05 mid, 65%) as having "still built" despite the guardrail. Re-derived from that trade's own persisted leg quotes: the `WIDE_NET_SPREAD` check did not exist until `5062f59`, one day after GS's 09-08 submission — not a live defect. Re-run against the CURRENT formula anyway: GS's round-trip width is 129.6% of \|net_mid\|, which the deployed 0.50 threshold already rejects outright if this chain recurred today. The audit's 65% figure is a one-way (mid-to-natural) percentage; round-trip = 2x one-way exactly (net_mid is algebraically the midpoint of net_natural and net_bidside), which also reconciles it with the 46%/88% figures row 19 was calibrated against — same round-trip metric, not two different ones. No change follows. |
| 33 | synthetic backtest's `iv_atm` forecast (`synthetic_chain.iv_forecast`, replacing replay.py's inline `rv_short * multiplier`) | pure short window (`BACKTEST_IV_TERM_WINDOW`-day `short_term_rv`, blend_weight=1.0 in effect) | blend of the short window and `RV_WINDOW=20` (`BACKTEST_IV_FORECAST_BLEND_WEIGHT = 0.3`) | 2026-09-10 | Proof supplied by the user, not derived here: under `iv = F*(1+k)` with `F` a no-lookahead forecast, expected edge per unit of vega is `-k*F`, uniform in the selection signal — Round 2's short-window-alone fix (row 28's sibling, S0 Task B) broke the ORIGINAL constant-vrp_ratio tautology but replaced it with a biased-forecast one: `rv_short` alone is a noisy, stale estimator of forward vol, so a VRP-ranked backtest priced off it measures that staleness, not a premium. `blend_weight=0.3` was chosen by comparison, not a formal grid search: `test_synthetic_chain_is_not_exploitable` (agent/tests/test_replay.py) regresses realized P&L on `vrp_ratio` across ~500 synthetic trades (fixed seed, real vol-persistence structure) using the REAL `_simulate()`, and the shipped weight passes a 3-standard-error neutrality bound (\|slope\|/stderr ≈ 0.15–2.0 across the values tried). **F3 amendment (docs/f1_f3_remediation_plan.md, 2026-09-10):** this row's original note gave the wrong reason for rejecting 0.0 — it said 0.0 "collapses `iv_atm` to `rv20` exactly," implying the same constant-`vrp_ratio` collapse Round 2 fixed. That claim was only ever true when `vrp_ratio`'s denominator was `rv20`; row 28 (committed before this row) already moved it to `rv_dte`, and directly measuring `blend_weight=0.0` against that denominator shows `vrp` sd=5.19, range 0.54–59.58, P(DEBIT)=24.0% — not constant, and DEBIT is *more* reachable at 0.0 than at 0.3 (19.0%) or 1.0 (15.5%), the opposite of what the original note implied. The real reason `blend_weight` stays > 0: it removes ALL of `iv_atm`'s cross-sectional variation from the short leg, an unjustified asymmetric choice with nothing to recommend it over any other value — and, structurally, as long as this harness's IV is built from the same estimators the live screen divides by, `vrp_ratio` can be made to vary or made neutral but never both (docs/f1_f3_remediation_plan.md §4); `w>0` is what buys DEBIT coverage at all, at the cost that DEBIT's resulting P&L is a known artifact, never a measured edge. Also fixed the SAME un-fixed tautology found live in `llm_replay.py` (`iv_atm = rv20 * multiplier`, never touched by row 28 — a second, independent instance of the exact bug pattern, confirming the "you'll grow a third" risk the user's message named). `TradeResult` gained a `vrp_ratio` field (`payoff.py`) and `payoff.pnl_vrp_regression` so this check is re-runnable, not just asserted once. The confirm-the-artifact diagnostic (window sweep at blend_weight=1.0, real market data) did NOT reproduce the predicted "DEBIT decays to zero as window→20" signature — see the session write-up; `vrp_ratio`'s denominator changed from `rv20` to `rv_dte` in row 28, before this row, which changes the sweep's mechanics from what the original algebraic argument assumed. Logged as a genuine open question, not smoothed over. |

| 34 | `ticker_screener.shortlist`'s `composite_score` normalisation, `vrp_lo`/`vrp_hi` | raw min/max over the scan's `data_ok` vrp_ratios | winsorized 10th/90th percentile (`statistics.quantiles(..., n=10)`), falling back to raw min/max below `len(ok_vrps) < 10` since a 4-element percentile is not a percentile | 2026-09-10 | docs/f1_f3_remediation_plan.md F1.4: min-max normalisation on a term weighted 0.70 (CREDIT) / 0.50 (DEBIT) in the composite lets one extreme `vrp_ratio` reading flatten every other candidate's score toward 0. Independent of F1's denominator revert — ships either way per the plan's sequencing note (S1). |
| 35 | `vrp_ratio`'s realized-vol denominator (`quant.compute_snapshot`) | DTE-matched window (`realised_vol_dte(closes, dte)`, row 28) | `RV_20` (`quant.realised_vol_20`) — reverts row 28 | 2026-09-10 | docs/f1_f3_remediation_plan.md F1, gated on `scripts/signal_forward_test.py`'s own validation (chain-free, n≈21,600, the validation row 28 was logged pending): `rv_20` has LOWER mean absolute error than `rv_dte` against actual forward realized vol at every DTE-band horizon, by 8–11% relative — the opposite of what row 28 was betting on. A second chain-free measurement (n=8,250 name-days) found `rv_dte`'s deviation from `rv_20` carries ZERO predictive content for forward vol (`FWD/rv_20` ≈ 1.00 across every bucket of that deviation; `FWD/rv_dte` swings 4.5×) — stronger than the MAE result, since it says the quantity `rv_dte` adds over `rv_20` is pure noise, not merely worse signal. `rv_dte` was also biased low (small-sample + its own 3–7-sample winsorisation) and ~3× higher relative variance, inflating `vrp_ratio`'s median ~41% and mean ~5× (Jensen/convexity in `1/x`). Measured post-revert (`python -m agent.backtest.replay`, 2026-03-12→2026-09-10, 361 settled trades): median `vrp_ratio` 1.31 (was ~1.62 pre-revert), `P(vrp_ratio > VRP_RATIO_CEILING=2.0)` = 3.3% (was 41.4%), CREDIT n=217 range [1.19, 2.36], DEBIT n=144 range [0.84, 1.00] — both regimes stay healthily populated, comfortably clear of the `VRP_CREDIT_MIN`/`VRP_DEBIT_MAX=1.00` sign guards, so none of the four downstream thresholds (`VRP_CREDIT_MIN`, `VRP_DEBIT_MAX`, `VRP_RATIO_CEILING`, `VRP_SHRINKAGE_FACTOR`) needed a value change — all four re-examined against this measurement and kept, per F1.3; only their comments, which cited `rv_dte`'s now-removed noise as the reason `VRP_RATIO_CEILING`/`VRP_SHRINKAGE_FACTOR` exist, were rewritten. `_RV_DTE_MIN` (the guard the DTE-matched path needed) deleted as unreferenced; `realised_vol_dte` itself kept — still called by `scripts/signal_forward_test.py`, the script that produced this row's own evidence. |

| 36 | `REAL_CHAIN_SPREAD_PCT` (backtest-only, `agent/backtest/real_chain.py` Path C) | *(introduced)* | `0.0441` | 2026-09-10 | docs/prompts/real_iv_surface_free.md Path C.2: historical option bars are trade prices, not quotes, so `real_chain.py`'s modeled bid/ask needs a spread constant the way `synthetic_chain.py`'s `BACKTEST_CHAIN_SPREAD_PCT = 0.03` already does. The prompt names `OptionQuotesRequest` for measuring it; the installed alpaca-py (0.42.0) has no historical option-quotes request class at all. Measured instead from the closest real substitute — the same live `OptionChainRequest` `ChainCache.load` already calls every cycle — across SPY/QQQ/AAPL/NVDA/AMD's real 3–7 DTE chains, feed=indicative: n=3158 usable quotes, median relative spread ((ask−bid)/mid) = 0.0441 (mean 0.1482, right-skewed by far-OTM wide quotes — median is the read, same shape `MAX_QUOTE_SPREAD_PCT`'s own comment describes for the live chain). A live-market measurement, not fit to any backtest P&L. Logged per row 33's precedent (a new, measured backtest-only constant is a trial the same way an introduced one is, even though it never touches the live decision path). |

**N = 36** recorded trials (rows above) against parameters or scoring terms read by the live
decision path, spanning 2026-08-29 through 2026-09-10. Row 4 is included even though the shipped
value is unchanged, because an alternative was proposed, evaluated against data, and rejected; that
is a trial by the paper's own definition (§9.2), not a no-op. Rows 26–28 are likewise counted despite
being explicitly framed as trials pending further validation, not settled calibrations — §9.2's
definition is about whether a search happened, not whether its outcome is yet proven. Rows 31–32
follow the same logic as row 4: a resize was considered and, on the evidence available, rejected.
Row 33's `BACKTEST_IV_FORECAST_BLEND_WEIGHT` is likewise a trial pending further validation, not a
settled calibration — it passed the neutrality test built alongside it, which is a different, weaker
claim than "0.3 is the right weight." Row 34 is F1.4 from docs/f1_f3_remediation_plan.md, which shipped
independently of the denominator revert per the plan's own sequencing note. Row 35 is F1.1–F1.3/F1.5
(the denominator revert itself, its measurement, and the threshold re-examination that measurement
gated) — F1's own §9.2 framing applies directly here: a revert of a prior trial (row 28) is itself a
new trial, one row, not a deletion of the old one; row 28 is left as-is, recording what was tried at
the time, per F1.6's explicit instruction. Row 36 is docs/prompts/real_iv_surface_free.md Path C's
one new backtest-only constant (`REAL_CHAIN_SPREAD_PCT`) — the real chain's IV surface itself is not
a parameter and is not logged here (a new data source is not a trial), but the one modeled constant
that surface still needs is, per row 33's identical precedent.

**Rows 17–25 backfilled 2026-09-10** (docs/strategy_audit_and_loop.md §3.1): the ledger previously
stopped at row 16 / N=16 (2026-08-29 through 2026-09-01) while 9 further performance-selected
trials had already shipped through 2026-09-10 — the ledger was trailing reality, not leading it,
which is the one thing `dsr.py`'s own comment says it must never do. Reconstructed the same way as
rows 1–16: `git log --all -p -- agent/config.py` plus each commit's own message/inline comments.

**Deliberately excluded — not performance-selected.** `WALK_CAP_MAX_FRACTION_OF_WIDTH`,
`WALK_CAP_MAX_FRACTION_OF_WIDTH_CLOSING`/`WALK_CAP_CREDIT_SIGN_FLOOR`, and `MAX_QUOTE_SPREAD_PCT`
are pure audit-driven safety bounds (arbitrage/no-worse-than-cash-settlement constraints, not values
selected by comparing outcomes) and are defensibly left out of `N`, per docs/report.md §9.2's
definition of a trial. Said explicitly here rather than silently dropped: including them would put
`N` at ~31, not 25.

This ledger is the `N` used by `agent/backtest/dsr.py`'s `deflated_sharpe(n_trials=...)` call —
see that file for how it is wired in, and `docs/preregistration.md` for why no row is added to
this table before Thursday close.
